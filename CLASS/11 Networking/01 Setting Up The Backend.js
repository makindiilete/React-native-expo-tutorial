/*
1.  Install the dependencies inside the backend folder
2.  Open 'config' folder >> 'development.json' >> Replace the ip address  of the assetsBaseUrl to your computer ip address
3.  Save and start the server with 'node index.js'
4.  Navigate to http://localhost:9000/api/listings to confirm all is working fine

NOTE : - We are using in memory database for faster setup which means we are not using a real database so anytime we restart the server, we loose all changes
*/
